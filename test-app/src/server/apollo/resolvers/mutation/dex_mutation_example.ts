/**
 * dex_mutation_example
 * @param parent
 * @param {InputType} args
 * @param ctx
 * @return {OutputType}
 */

export const dex_mutation_example = async (_, args: any, ctx: any) => {
	try {
	} catch (e) {
		throw e;
	}
};
